{
  "version": 2,
  "builds": [
    {
      "src": "frontend",
      "use": "npm run build",
      "config": {
        "outputDirectory": "dist/frontend"
      }
    },
    {
      "src": "backend",
      "use": "npm run build",
      "config": {
        "outputDirectory": "dist/backend"
      }
    }
  ]
}
